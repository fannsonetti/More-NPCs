using UnityEngine;

namespace MoreNPCs.Supervisor
{
    /// <summary>Shared config for supervisor NPCs.</summary>
    public static class SupervisorConfig
    {
        public static readonly Vector3 DefaultSpawnPosition = new Vector3(0f, -2f, 0f);
    }
}
